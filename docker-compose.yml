version: "3.8"
services:
  postgres:
    image: postgres:15-alpine
    container_name: fly-in-high-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: fly_in_high
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: fly-in-high-redis
    restart: unless-stopped
    ports:
      - "6379:6379"

volumes:
  pgdata:

